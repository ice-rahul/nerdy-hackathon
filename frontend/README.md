# LinguaBuild frontend

Next.js (App Router) + Tailwind + TypeScript client for LinguaBuild. See the [root README](../README.md) for setup, environment variables, and deploy instructions — this app requires a running `backend/` instance (`NEXT_PUBLIC_SERVICE_URL`) to generate any exercise content.

```bash
npm install
npm run dev
```
